@import '../vendor/normalize.css';

body {
    div {
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    h1 {
        text-align: center;
        margin: 30px auto;
    }

    h2 {
        text-align: center;
        margin-bottom: 30px;
    }

    img {
        width: 100%;
        max-width: 500px;
        margin-bottom: 30px;
    }
}